apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-test
  namespace: default
spec:
  selector:
    matchLabels:
      app: gitops-test
  template:
    metadata:
      labels:
        app: gitops-test
    spec:
      containers:
      - image: ghcr.io/activa-prefapp/gitops-app-test:d8b5e60a0adac390d8b5daefc0f02b048c2d71d8
        imagePullPolicy: IfNotPresent
        name: gitops-test
        ports:
        - containerPort: 80
          protocol: TCP
        resources:
          limits:
            cpu: 100m
            memory: 250Mi
          requests:
            cpu: 100m
            memory: 250Mi
      imagePullSecrets:
        - name: git-registry-secret